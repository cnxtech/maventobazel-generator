# This is the root level WORKSPACE file

# since this tool is a small extraction from our Bazel monorepo, the WORKSPACE is empty for this Git repo

# load our migrated dependencies
load("//:external_deps.bzl", "external_maven_jars")
external_maven_jars()
