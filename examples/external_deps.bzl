# This is the default empty deps file
#   The migration tool will generate this file with external deps if you run the examples

def external_maven_jars():
  # noop, run the examples to generate a file with deps
  print("No external dependencies have been declared yet in external_deps.bzl")
